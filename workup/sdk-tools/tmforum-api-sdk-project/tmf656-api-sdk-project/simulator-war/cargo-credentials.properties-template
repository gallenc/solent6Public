# cargo-credentials.properties
# this properties file template should be copied to cargo-credentials.propeties
# this properties file sets the credentials for the cargo-maven-plugin to deploy a war to a remote instance
# git ignore must be set to prevent cargo-credentials.propeties being checked into a repository

cargo.hostname=127.0.0.1
cargo.servlet.port=8080
cargo.remote.username=someusername
cargo.remote.password=somepassword